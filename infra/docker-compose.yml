services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: proofsource
      POSTGRES_PASSWORD: proofsource
      POSTGRES_DB: proofsource
    ports: ["5432:5432"]
  api:
    build: ../apps/api
    env_file: ../.env
    depends_on: [db]
    ports: ["3000:3000"]
